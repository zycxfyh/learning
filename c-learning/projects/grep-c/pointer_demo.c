#include <stdio.h>

int main() {
    // 1. 建一所房子 (定义变量)
    int house = 100;

    // 2. 拿到这所房子的门牌号 (取地址 &)
    // 3. 把门牌号写在一张纸条上 (定义指针 *)
    int *paper = &house;

    printf("=== 🏠 房子与门牌号演示 ===\n");
    printf("我的房子(house)里住着: %d\n", house);
    // %p 是专门用来打印地址(Pointer)的格式
    printf("房子的门牌号是(地址): %p\n", &house); 

    printf("\n=== 📄 纸条(指针)演示 ===\n");
    printf("纸条(paper)上写的内容是: %p (看！它就是house的门牌号)\n", paper);
    
    // *paper 的意思是：去这个地址看看
    printf("拿着纸条去找房子(*paper): %d (看！找到了house里的人)\n", *paper);

    printf("\n=== 🪄 魔法演示(通过指针修改) ===\n");
    printf("正在通过纸条修改房子里的内容...\n");
    *paper = 999; // 我没有碰到 'house' 这个变量名，我是通过地址改的

    printf("现在的房子(house)变成了: %d\n", house);

    return 0;
}
